{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. Keep this minimal: grant only what the UI actually uses.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
