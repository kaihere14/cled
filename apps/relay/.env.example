# Copy to `.env` (git-ignored) and fill in. See README.md, "Configuration".
# Server-only secret: never commit it or share it.
CLERK_SECRET_KEY=
# Public values.
CLERK_PUBLISHABLE_KEY=
CLERK_OAUTH_CLIENT_ID=
# Optional: PEM public key for verifying tokens without fetching Clerk's signing keys.
# CLERK_JWT_KEY=
